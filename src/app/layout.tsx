import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NS Engineering Works - Generator Solutions | Dealing in Generators, Rentals, Maintenance",
  description: "NS Engineering Works provides comprehensive generator solutions including dealing in generators (buying and selling), rentals, maintenance, and repair services. From 100KVA to megawatts, diesel, petrol, and gas generators.",
  keywords: "generators, industrial generators, generator dealers, generator rental, generator maintenance, generator repair, diesel generators, NS Engineering Works",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
